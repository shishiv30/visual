<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface">

    <include
        android:id="@+id/page_list"
        layout="@layout/page_list" />

    <include
        android:id="@+id/page_capture"
        layout="@layout/page_capture"
        android:visibility="gone" />

    <include
        android:id="@+id/page_prepare"
        layout="@layout/page_prepare"
        android:visibility="gone" />

    <include
        android:id="@+id/page_player"
        layout="@layout/page_player"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/loading"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/surface"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ProgressBar
            android:id="@+id/loading_spinner"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:indeterminateTint="@color/accent_blue" />

        <TextView
            android:id="@+id/loading_caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_text"
            android:textColor="@color/on_surface"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
